在Gralde中创建自定义插件，Gradle提供了三种方式:
a.在build.gradle脚本中直接使用
在Gradle中创建自定义插件，Gradle提供了三种方式:
a.在build.gradle脚本中直接使用
b.在buildSrc文件夹中使用
c.在独立Module中使用


注意事项:
a.通过buildSrc方式自定义插件，使用apply plugin方法导入时候，不能加单引号，而且类要是完整的包路径
